{
  "name": "extension",
  "version": "1.0.0",
  "description": "",
  "main": "background.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "vite build && pwsh -Command \"Copy-Item -Path manifest.json -Destination dist/ -Force; Copy-Item -Path icons -Destination dist/ -Recurse -Force\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@xenova/transformers": "^2.17.2",
    "face-api.js": "^0.22.2",
    "onnxruntime-web": "^1.29.0"
  }
}
